export interface ServiceTypeProps {
    serviceTypeId: string, // là serviceTypeId của serviceName trong thuộc tính children
    serviceName: string, // là của serviceName trong thuộc tính children
    serviceType: string, //là serviceName cùng cấp với thuộc tính children
}